Here are some refinement tasks that I'd like to address once the core features are completed.

1. Player can only search once at each location. Search should be deterministic either there is a hidden passage or not, this should be determined when generating the map.

2. Action results should be more prominent in the UI, move to the top.
