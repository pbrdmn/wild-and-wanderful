Here are some refinement tasks that I'd like to address once the core features are completed.

1. It is hard to view the map on mobile, the grid is too small. Maybe there should be a "zoom" function, so the player initially only sees their current space and 2 tiles in each direction (total 25 squared), but can toggle to show the whole map.

2. The action buttons at the bottom of the screen are too wide for mobile layout and extend off the screen, perhaps it should be an multiple rows so the buttons can be larger and still fit on the screen?

3. From the description screen, with description of the directions, allow the player to click on those directions to move immediately without opening the map.

4. Clean up the terminology between characters / heros. Let's call them "Wanderers"

5. Remove the idea of turns outside of combat to make the gameplay smoother.